# Secrets — never commit. Use .env.example as the template.
.env
.env.*
!.env.example

# Local model weights (large, re-downloadable)
models/*
!models/.gitkeep

# Volume backups contain .env and database dumps
backups/

# Agent/session scratch and local tool state - not part of the stack
.remember/
.claude/
.cache/
.deploytest/

# Runtime logs written into config dirs by the services themselves
config/homepage/logs/

# OS / editor noise
Thumbs.db
.DS_Store
*.swp
.idea/
.vscode/

# TLS private keys and basic-auth hashes generated by scripts/gen-certs
config/traefik/certs/*
!config/traefik/certs/.gitkeep
config/traefik/auth/*
!config/traefik/auth/.gitkeep

# Authelia: generated secrets, OIDC signing key and user password hashes
config/authelia/secrets/*
config/authelia/clients.yml
# team.yml IS committed on purpose - it is the reviewable access list and
# contains no secrets. users.yml holds the password hashes and is not.
config/authelia/users.yml

# Issued API keys - never commit
.keys/

# packaging scratch and built release artefacts (hundreds of MB, and
# rebuildable with scripts/package.sh)
.pkgstage/
dist/

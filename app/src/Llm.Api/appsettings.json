{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.EntityFrameworkCore": "Warning",
      "Microsoft.EntityFrameworkCore.Database.Command": "None",
      "System.Net.Http.HttpClient": "Warning"
    }
  },
  "AllowedHosts": "*",
  "///": "HttpClient logs two lines per request at Information; the engine is polled every 10 s. Failures surface as exceptions and the app's own warnings.",
  "//": "EF's command logger is off: it re-reports failures that already surface as exceptions, and on first boot it logs an 'Error' for the migrations table that does not exist yet."
}

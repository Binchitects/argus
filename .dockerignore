# The image builds from source: the frontend (npm ci) and the backend (restore,
# test, publish). Nothing prebuilt, secret or large crosses into the context.
.git
.github
.claude
.remember

dotnet/**/bin/
dotnet/**/obj/
dotnet/native/
frontend/node_modules/
frontend/dist/
frontend/test-results/
frontend/playwright-report/
frontend/e2e/.work/

stack/.env
stack/.env.*
stack/models/
stack/backups/
stack/config/argus/tls/*
data/
*.db
*.db-wal
*.db-shm

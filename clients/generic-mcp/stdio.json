{
  "mcpServers": {
    "argus": {
      "command": "argus",
      "args": ["serve", "--config", "/etc/argus/config.yaml", "--stdio"],
      "env": {
        "ARGUS_TOKEN": "<your-gitlab-token>"
      }
    }
  }
}

# Continue -> Argus. Merge into ~/.continue/config.yaml.
# The code index key comes from Settings & keys in the app.
mcpServers:
  - name: argus
    type: streamable-http
    url: http://llm.example.lan:8080/mcp
    requestOptions:
      headers:
        Authorization: Bearer ak_your_code_index_key
